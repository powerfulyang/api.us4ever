// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"api.us4ever/internal/ent/keep"
	"api.us4ever/internal/ent/user"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Keep is the model entity for the Keep schema.
type Keep struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Content holds the value of the "content" field.
	Content string `json:"content,omitempty"`
	// Summary holds the value of the "summary" field.
	Summary string `json:"summary,omitempty"`
	// IsPublic holds the value of the "isPublic" field.
	IsPublic bool `json:"isPublic,omitempty"`
	// Tags holds the value of the "tags" field.
	Tags json.RawMessage `json:"tags,omitempty"`
	// Views holds the value of the "views" field.
	Views int32 `json:"views,omitempty"`
	// Likes holds the value of the "likes" field.
	Likes int32 `json:"likes,omitempty"`
	// ExtraData holds the value of the "extraData" field.
	ExtraData json.RawMessage `json:"extraData,omitempty"`
	// Category holds the value of the "category" field.
	Category string `json:"category,omitempty"`
	// OwnerId holds the value of the "ownerId" field.
	OwnerId string `json:"ownerId,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// UpdatedAt holds the value of the "updatedAt" field.
	UpdatedAt time.Time `json:"updatedAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the KeepQuery when eager-loading is set.
	Edges        KeepEdges `json:"edges"`
	selectValues sql.SelectValues
}

// KeepEdges holds the relations/edges for other nodes in the graph.
type KeepEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e KeepEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Keep) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case keep.FieldTags, keep.FieldExtraData:
			values[i] = new([]byte)
		case keep.FieldIsPublic:
			values[i] = new(sql.NullBool)
		case keep.FieldViews, keep.FieldLikes:
			values[i] = new(sql.NullInt64)
		case keep.FieldID, keep.FieldTitle, keep.FieldContent, keep.FieldSummary, keep.FieldCategory, keep.FieldOwnerId:
			values[i] = new(sql.NullString)
		case keep.FieldCreatedAt, keep.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Keep fields.
func (k *Keep) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case keep.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				k.ID = value.String
			}
		case keep.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				k.Title = value.String
			}
		case keep.FieldContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value.Valid {
				k.Content = value.String
			}
		case keep.FieldSummary:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field summary", values[i])
			} else if value.Valid {
				k.Summary = value.String
			}
		case keep.FieldIsPublic:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field isPublic", values[i])
			} else if value.Valid {
				k.IsPublic = value.Bool
			}
		case keep.FieldTags:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field tags", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &k.Tags); err != nil {
					return fmt.Errorf("unmarshal field tags: %w", err)
				}
			}
		case keep.FieldViews:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field views", values[i])
			} else if value.Valid {
				k.Views = int32(value.Int64)
			}
		case keep.FieldLikes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field likes", values[i])
			} else if value.Valid {
				k.Likes = int32(value.Int64)
			}
		case keep.FieldExtraData:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field extraData", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &k.ExtraData); err != nil {
					return fmt.Errorf("unmarshal field extraData: %w", err)
				}
			}
		case keep.FieldCategory:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field category", values[i])
			} else if value.Valid {
				k.Category = value.String
			}
		case keep.FieldOwnerId:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ownerId", values[i])
			} else if value.Valid {
				k.OwnerId = value.String
			}
		case keep.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				k.CreatedAt = value.Time
			}
		case keep.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				k.UpdatedAt = value.Time
			}
		default:
			k.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Keep.
// This includes values selected through modifiers, order, etc.
func (k *Keep) Value(name string) (ent.Value, error) {
	return k.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Keep entity.
func (k *Keep) QueryUser() *UserQuery {
	return NewKeepClient(k.config).QueryUser(k)
}

// Update returns a builder for updating this Keep.
// Note that you need to call Keep.Unwrap() before calling this method if this Keep
// was returned from a transaction, and the transaction was committed or rolled back.
func (k *Keep) Update() *KeepUpdateOne {
	return NewKeepClient(k.config).UpdateOne(k)
}

// Unwrap unwraps the Keep entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (k *Keep) Unwrap() *Keep {
	_tx, ok := k.config.driver.(*txDriver)
	if !ok {
		panic("ent: Keep is not a transactional entity")
	}
	k.config.driver = _tx.drv
	return k
}

// String implements the fmt.Stringer.
func (k *Keep) String() string {
	var builder strings.Builder
	builder.WriteString("Keep(")
	builder.WriteString(fmt.Sprintf("id=%v, ", k.ID))
	builder.WriteString("title=")
	builder.WriteString(k.Title)
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(k.Content)
	builder.WriteString(", ")
	builder.WriteString("summary=")
	builder.WriteString(k.Summary)
	builder.WriteString(", ")
	builder.WriteString("isPublic=")
	builder.WriteString(fmt.Sprintf("%v", k.IsPublic))
	builder.WriteString(", ")
	builder.WriteString("tags=")
	builder.WriteString(fmt.Sprintf("%v", k.Tags))
	builder.WriteString(", ")
	builder.WriteString("views=")
	builder.WriteString(fmt.Sprintf("%v", k.Views))
	builder.WriteString(", ")
	builder.WriteString("likes=")
	builder.WriteString(fmt.Sprintf("%v", k.Likes))
	builder.WriteString(", ")
	builder.WriteString("extraData=")
	builder.WriteString(fmt.Sprintf("%v", k.ExtraData))
	builder.WriteString(", ")
	builder.WriteString("category=")
	builder.WriteString(k.Category)
	builder.WriteString(", ")
	builder.WriteString("ownerId=")
	builder.WriteString(k.OwnerId)
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(k.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(k.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Keeps is a parsable slice of Keep.
type Keeps []*Keep
